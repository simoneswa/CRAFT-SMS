# This workflow is disabled — no webpack build at repo root.
# Frontend is built via pnpm in deploy-gcp.yml.
name: NodeJS with Webpack

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is intentionally disabled."
