[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT --app-dir CRAFT_SMS/backend --proxy-headers --forwarded-allow-ips='*'"
restartPolicy = "always"

[env]
PYTHON_VERSION = "3.10"
